<script src="{{=URL('static','js/mermaid.min.js')}}"></script>
<script src="{{=URL('static','js/mermaid-helpers.js', vars=dict(v=static_cachebust('js/mermaid-helpers.js')))}}"></script>

{{if options == 'dashboard':}}

<div>
    {{=A("Edit", _href=URL('diagram', 'editmodeldiagram.html', args=model_id ), _class="btn btn-primary")}}

    <div style="overflow-x:auto"><div id="graph-container" class="col"></div></div>
</div>

{{else:}}
<div class="card">
    <div class="card-header">
        <h5 class="card-title" style="display: inline-block;">
            <span class="card-icon">{{=controller_icon('diagram', 25)}}</span>
            Wiring Diagram
        </h5>
        {{=A("Edit", _href=URL('diagram', 'editmodeldiagram.html', args=model_id ), _class="card-link th-new-link")}}

    </div>

    <div class="card-body">

        <div style="overflow-x:auto"><div id="graph-container" class="col"></div></div>

    </div>

</div>
{{pass}}


<script>

    const mermaidGraph = `{{=XML(mermaid)}}`;
    const edgeStyles = {{=XML(edge_styles_json)}};
    const container = document.getElementById("graph-container");

    if (mermaidGraph.trim()) {
        renderMermaidInto(container, mermaidGraph, { edgeStyles });
    } else {
        container.textContent = 'No diagram yet.';
    }

</script>
